<x-legal.app-privacy platform="macOS">
    <x-slot:storage>
        <p>On macOS this data lives in the app's own container under your user account (<code>~/Library</code>), protected by your macOS user permissions like any app's data. Nothing is written outside it.</p>
    </x-slot:storage>
    <x-slot:deletion>
        <p>Quit the app and move it from Applications to the Trash, then remove its container folder under <code>~/Library</code> if you want the saved server address and downloads gone too. Signing out inside the app ends the session with your server.</p>
    </x-slot:deletion>
</x-legal.app-privacy>
