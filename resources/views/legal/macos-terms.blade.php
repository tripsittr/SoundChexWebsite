<x-legal.app-terms platform="macOS">
    <h2>5. macOS specifics</h2>
    <ul>
        <li>Current builds are not yet notarized with Apple; opening them past Gatekeeper (<a href="{{ route('docs.show', 'dmg-setup') }}">documented here</a>) is your informed choice. The source is public precisely so that choice can be informed.</li>
        <li><strong>SoundChex Server.app</strong> — the host application with service controls — is covered by the <a href="{{ route('legal.show', 'server-terms') }}">Server Terms</a>, since it manages the server on the machine it runs on.</li>
    </ul>
</x-legal.app-terms>
