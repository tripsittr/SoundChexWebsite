<x-legal.app-terms platform="iPadOS" :available="false">
    <h2>5. iPadOS specifics</h2>
    <ul>
        <li>A dedicated iPad app is planned; today the iPad runs the <a href="{{ route('docs.show', 'app-ipados') }}">iOS build or the web app</a>. Until the dedicated app ships, use of the iOS app on iPad is governed by the <a href="{{ route('legal.show', 'ios-terms') }}">iOS terms</a>, which are materially identical to these.</li>
        <li>When it reaches the App Store or TestFlight, Apple's standard end-user terms will apply alongside — not instead of — the MIT License and these terms.</li>
    </ul>
</x-legal.app-terms>
