# 031 — New brand icon (dark default)

*2026-09-18.*

## Done

Updated the site's `favicon.ico` and `app-icon.png` / `app-icon-128.png` to the
new SoundChex mark, dark variant (the red headphones-and-waveform on a black
tile) — matching the app-icon change across the desktop and iOS apps. Icon
assets only; the in-page logos are unchanged.
