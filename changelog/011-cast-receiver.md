# 011 — Cast-receiver on the roadmap (W-23)

*2026-09-17.*

## Done

- Roadmap: added **Cast to SoundChex (receiver)** — make SoundChex a target other
  devices can cast TO (Google Cast receiver + DLNA renderer). Noted that AirPlay
  receiver is Apple-restricted to hardware, so it isn't a realistic app target.
- Clarified the existing cast items as **(send)** to distinguish sender from the
  new receiver.

## Why

Requested as a must-have. Sender (cast out) and receiver (be cast to) are
separate features; the receiver's realistic path is Google Cast + DLNA rendering.
Pairs with app/server work (tracked app-side).
